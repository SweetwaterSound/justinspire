function justInspire()
{
    const words = {
        verbs: [],
        greater: [],
        object: [],
        lesser: [],
        with: [],
    };
    
    
    // return words[whichWord][Math.floor(Math.random() * words[whichWord].length)];
    
    return "";
}
